---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: docompositedefinitionrevisions.do.pulumi.com
spec:
  group: do.pulumi.com
  names:
    kind: DoCompositeDefinitionRevision
    listKind: DoCompositeDefinitionRevisionList
    plural: docompositedefinitionrevisions
    shortNames:
    - docdrev
    singular: docompositedefinitionrevision
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.definitionName
      name: DEFINITION
      type: string
    - jsonPath: .spec.revision
      name: REVISION
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          DoCompositeDefinitionRevision is an immutable snapshot of a
          DoCompositeDefinition. Editing a definition creates the next revision;
          composites render from the revision their update policy selects, so a
          definition edit never silently rewrites pinned instances.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              DoCompositeDefinitionRevisionSpec is the immutable snapshot of one
              definition generation.
            properties:
              definition:
                description: Definition is the snapshotted definition spec.
                properties:
                  api:
                    description: |-
                      API exposes this definition as its own typed, namespaced CRD (e.g.
                      `kind: Website` in `platform.acme.com`): users apply the platform
                      kind with their parameters as spec, instead of a generic DoComposite.
                      Each typed object is translated into an owned DoComposite.
                    properties:
                      deprecatedVersions:
                        description: |-
                          DeprecatedVersions are previously served versions kept served (and
                          marked deprecated) while their objects migrate. Remove a version
                          only when the definition's status reports zero objects for it.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      group:
                        description: |-
                          Group of the generated API (e.g. platform.acme.com). Must be on the
                          operator's install-time allowlist (Helm value compositeApiGroups),
                          which also renders the matching manager RBAC. Empty uses the fixed
                          typed.do.pulumi.com group.
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)+$
                        type: string
                      kind:
                        description: Kind of the generated API (e.g. Website).
                        pattern: ^[A-Z][A-Za-z0-9]*$
                        type: string
                      parametersSchema:
                        description: |-
                          ParametersSchema is the OpenAPI v3 schema validating the typed
                          object's spec — the single source of the parameter contract. It also
                          validates DoComposite parameters at render time and template
                          ${params.*} usage at apply time. Empty accepts any object. The
                          property name "doplane" is reserved for doplane's lifecycle knobs.
                        x-kubernetes-preserve-unknown-fields: true
                      plural:
                        description: Plural resource name; defaults to lowercase(kind)
                          + "s".
                        type: string
                      version:
                        description: |-
                          Version served and referenced by the templates (storage version).
                          Defaults to v1alpha1. Bumping it keeps prior versions only if they
                          are listed in deprecatedVersions; generated CRDs use conversion
                          strategy None, so every served version must stay round-trippable —
                          a new required parameter is a new API, not a new version.
                        pattern: ^v[0-9]+((alpha|beta)[0-9]+)?$
                        type: string
                    required:
                    - kind
                    type: object
                    x-kubernetes-validations:
                    - message: spec.api.kind is immutable
                      rule: self.kind == oldSelf.kind
                    - message: spec.api.plural is immutable
                      rule: has(self.plural) == has(oldSelf.plural) && (!has(self.plural)
                        || self.plural == oldSelf.plural)
                    - message: spec.api.group is immutable
                      rule: has(self.group) == has(oldSelf.group) && (!has(self.group)
                        || self.group == oldSelf.group)
                  resources:
                    description: Resources are the templates rendered into DoResources.
                    items:
                      description: CompositeResourceTemplate templates one DoResource
                        of a composite.
                      properties:
                        deletionPolicy:
                          description: DeletionPolicy for the child resource. Defaults
                            to Delete.
                          enum:
                          - Delete
                          - Orphan
                          type: string
                        externalName:
                          description: |-
                            ExternalName adopts an existing external resource instead of creating
                            one: rendered (params/self expressions allowed; sibling resources.*
                            are not) into the child's crossplane.io/external-name annotation.
                          type: string
                        name:
                          description: |-
                            Name identifies the resource within the composite; it is referenced
                            by sibling expressions and suffixes the child object's name.
                          pattern: ^[a-z0-9]([a-z0-9-]*[a-z0-9])?$
                          type: string
                        package:
                          description: Package pins the provider package (e.g. "aws@7.34.0").
                          type: string
                        properties:
                          description: |-
                            Properties are the resource inputs; string values may contain
                            expressions (see DoCompositeDefinitionSpec).
                          x-kubernetes-preserve-unknown-fields: true
                        providerRef:
                          description: |-
                            ProviderRef resolves package and credentials from a cluster-scoped
                            DoProvider profile; copied verbatim into the child DoResource.
                          properties:
                            kind:
                              default: DoProvider
                              description: |-
                                Kind of the profile: DoProvider (cluster-scoped) or DoProviderConfig
                                (same namespace as this resource).
                              enum:
                              - DoProvider
                              - DoProviderConfig
                              type: string
                            name:
                              description: Name of the profile object.
                              minLength: 1
                              type: string
                          required:
                          - name
                          type: object
                        type:
                          description: Type is the Pulumi resource type token.
                          minLength: 1
                          type: string
                      required:
                      - name
                      - type
                      type: object
                    minItems: 1
                    type: array
                required:
                - resources
                type: object
                x-kubernetes-validations:
                - message: spec.api cannot be removed once set; delete the definition
                    instead
                  rule: '!has(oldSelf.api) || has(self.api)'
              definitionName:
                description: DefinitionName is the DoCompositeDefinition this revision
                  snapshots.
                minLength: 1
                type: string
              revision:
                description: |-
                  Revision numbers the snapshot, monotonically increasing per
                  definition.
                format: int64
                minimum: 1
                type: integer
            required:
            - definition
            - definitionName
            - revision
            type: object
        type: object
        x-kubernetes-validations:
        - message: revisions are immutable
          rule: self.spec == oldSelf.spec
    served: true
    storage: true
    subresources: {}
